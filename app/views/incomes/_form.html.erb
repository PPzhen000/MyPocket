<%= form_for ([@user, @user.incomes.build]) do |f| %>
  <% @income = @user.incomes.build%>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.label :amount %>
  <%= f.text_field :amount %>
  <%= f.submit %>
<% end %>
